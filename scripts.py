import numpy as np


def compute(dt, Qw, A, K_h, K_v, hw, R, r_w, zspan, L_w, n, theta_i, Z, PSI_i, WI, eps_R, eps_h, RK):
    # todo: translate matlab code...
    return None
    # return next_hw,next_R,next_Q_spill,next_Z,next_zR,dt2
